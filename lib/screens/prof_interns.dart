import 'package:flutter/material.dart';

class ProffesorInterns extends StatelessWidget {
  const ProffesorInterns({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
